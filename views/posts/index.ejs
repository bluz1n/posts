<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans&display=swap" rel="stylesheet">
  <link href="styles.css" rel="stylesheet" type="text/css">
  <title>blog</title>
</head>

<body>

  <div class="container">

    <h1>posts</h1>

    <a href="/posts/new" class="button">New post</a>

    <% posts.forEach(post=> { %>
      <div class="posts-container">
        <div class="posts-body">
          <h4 class="posts-title">
            <%= post.title %>
          </h4>
          <div class="posts-subtitle">
            <%= post.created.toLocaleDateString() %>
          </div>
          <div class="post-text">
            <p>
              <%= post.description %>
            </p>
          </div>

          <a class="button-read" href="posts/<%= post.slug %>">Read more</a>
          <a class="button-read" href="posts/edit/<%= post.id %>">Edit</a>


          <form action="/posts/<%= post.id %>?_method=DELETE" method="POST" class="form-inline">
            <button class="button-delete" type="submit">Delete</button>
          </form>

        </div>

      </div>
      <% }) %>
  </div>
</body>

</html>